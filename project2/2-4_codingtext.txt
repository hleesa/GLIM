#include <string>
#include <vector>

using namespace std;

int solution(int a, int b) {
    const int ab = stoi(to_string(a) + to_string(b));
    const int ba = stoi(to_string(b) + to_string(a));
    return max(ab, ba);
}
